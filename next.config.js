/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true,
  },
  // Enable static export if you want to deploy as static site
  // output: 'export',
}

module.exports = nextConfig
